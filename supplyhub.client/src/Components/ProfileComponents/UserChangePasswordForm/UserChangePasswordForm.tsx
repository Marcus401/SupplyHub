import React from 'react'

type Props = {};

const UserChangePasswordForm = (props: Props) => {
    return (
        <div>UserChangePasswordForm</div>
    );
};

export default UserChangePasswordForm;
